---
export type Variant = "primary" | "secondary" | "tertiary" | "ghost";
export type Size = "sm" | "md" | "lg";

type Props = {
  href?: string;
  external?: boolean;
  variant?: Variant;
  size?: Size;
  disabled?: boolean;
  autoFocus?: boolean;
} & Record<string, unknown>;

const {
  href,
  external = false,
  variant = "primary",
  size = "md",
  disabled = false,
  autoFocus = false,
  class: classNameOverride = "",
  ...rest
} = Astro.props as Props;

const baseClass = `btn btn--${variant} btn--${size}`;
const className = [baseClass, classNameOverride].filter(Boolean).join(" ");
---

{
  href ? (
    <a
      class={className}
      href={href}
      target={external ? "_blank" : undefined}
      rel={external ? "noopener noreferrer" : undefined}
      {...rest}
    >
      <slot />
    </a>
  ) : (
    <button
      class={className}
      type="button"
      disabled={disabled}
      autofocus={autoFocus}
      {...rest}
    >
      <slot />
    </button>
  )
}

<style>
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    border-radius: var(--radius-2);
    text-decoration: none;
    font-weight: 700;
    border: 1px solid transparent;
    cursor: pointer;
    min-height: var(--target-min);
    transition:
      background-color 160ms ease,
      color 160ms ease,
      border-color 160ms ease,
      transform 160ms ease,
      box-shadow 160ms ease;
  }

  .btn--sm {
    font-size: var(--font-size-0);
    padding: var(--space-2) var(--space-3);
  }

  .btn--md {
    font-size: var(--font-size-1);
    padding: var(--space-2) var(--space-4);
  }

  .btn--lg {
    font-size: var(--font-size-2);
    padding: var(--space-3) var(--space-6);
  }

  .btn--primary {
    background: var(--color-cta-primary);
    color: var(--color-white);
    box-shadow: 0 12px 28px rgba(18, 7, 14, 0.35);
    border-color: transparent;
  }

  .btn--primary:hover {
    background: var(--color-cta-primary-dark);
    transform: translateY(-1px);
  }

  .btn--secondary {
    background: var(--color-cta-secondary);
    border-color: var(--color-section-border);
    color: var(--color-primary);
  }

  .btn--secondary:hover {
    background: var(--color-section-surface);
  }

  .btn--tertiary {
    background: transparent;
    border-color: transparent;
    color: var(--color-link-muted);
    text-decoration: underline;
    text-underline-offset: 0.18em;
    text-decoration-thickness: 0.18em;
  }

  .btn--tertiary:hover {
    color: var(--color-primary);
    text-decoration-thickness: 0.21em;
  }

  .btn--ghost {
    background: transparent;
    border-color: var(--color-section-border);
    color: var(--color-primary);
  }

  .btn--ghost:hover {
    background: var(--color-gray-50);
  }

  .btn:disabled {
    opacity: 0.65;
    cursor: not-allowed;
  }
</style>
